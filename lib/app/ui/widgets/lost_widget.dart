import 'package:flutter/material.dart';

class LostWidget extends Container {
  LostWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        Text('Achados Recentes'),
      ],
    );
  }
}
